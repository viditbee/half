- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: Build the SourceStore port (AD-13) — protocol plus content-addressed local filesystem implementation for layer 1.
  evidence: Split from story 1 at the token-count checkpoint. Nothing reads or writes sources until ingestion (story 3), and no story-1 acceptance criterion touches it. AD-13 lives in the spine, so the invariant binds whenever the code lands.

- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: Build the single projection renderer (AD-31) for `loops/*.md` and `people/*.md`.
  evidence: Split from story 1 at the token-count checkpoint. No story-1 acceptance criterion mentions projections; the first real consumer is whatever displays a loop. AD-31 binds from the spine regardless of when the renderer is written.

- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: Store.append re-folds the whole log and rewrites the entire database on every record — O(n) per append, O(n²) over a store's lifetime.
  evidence: Blind hunter measured 300 appends at 0.33s locally. The wholesale-rebuild docstring defends rebuilding the derived view wholesale, but never addresses doing it per append, on a design explicitly targeting a cheap self-hosted box and a log meant to accumulate for years. Wants a batch-append API or a deferred-rebuild option, plus a documented bound.

- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: The derived view carries no staleness marker, so nothing can cheaply tell whether half.db reflects the current log.
  evidence: State records no log offset, generation or content hash. Every read trusts the database blindly and the only remedy is a full rebuild. Relevant because expunge_bodies rewrites shards behind the database's back.

- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: The fold discards the retract/revise distinction it deliberately preserves in the log.
  evidence: Both branches pop the target, so State carries no record of what was retracted versus revised. The consumer that must compose "Half was wrong" versus "you changed" has to full-scan every shard, with no index and no derived table.

- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: No record-id uniqueness enforcement anywhere in the log.
  evidence: Nothing in decode, append or fold detects duplicate ids, which undermines using ids as the fold's keys. Store.expunge previously minted colliding ids; that specific collision is fixed, but the general invariant is unguarded.

- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: No CI workflow, lint, or type-check configuration.
  evidence: The codebase is fully annotated and leans hard on static tests (test_purity.py is an AST checker), but nothing runs them automatically and there is no ruff or mypy config. The replay and purity tests are described as CI-enforced in AD-4 and AD-30; no CI exists yet.

- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: Packaging metadata is thin — no readme, license, authors, classifiers, py.typed marker, or runtime __version__.
  evidence: Blind hunter noted the package is fully annotated but ships no py.typed, and 0.1.0 lives only in pyproject.toml with no __version__ to read at runtime.

- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: DELETE FROM belief_fts is not SQLite's documented idiom for an external-content FTS5 table.
  evidence: The contract for content='beliefs' is INSERT INTO belief_fts(belief_fts) VALUES('delete-all'). The current statement order happens to satisfy the plain DELETE and an integrity-check passes, but reordering the two loops inside rebuild would silently corrupt the index — an undocumented dependency on statement order.

- source_spec: `_bmad-output/specs/spec-half/stories/1-store-substrate.md`
  summary: RESOLVED (CI portion) — .github/workflows/ci.yml now runs the suite on 3.12 and 3.13 plus four named invariant gates (AD-4 replay, AD-30 purity, AD-2 stdlib-only runtime, AD-11 tree scans clean).
  evidence: Closes the CI half of the earlier "no CI workflow, lint, or type-check configuration" entry, which that entry's own rule forbids editing in place. Lint and type-check configuration (ruff, mypy) remain deferred and unaddressed.

- source_spec: `_bmad-output/specs/spec-half/stories/2-channel-and-thread.md`
  summary: Build the WhatsApp Cloud API adapter behind the existing Channel port — webhook receiver with ack-before-process (AD-23), the 24-hour window arithmetic, and the approved-template path for unprompted contact outside it.
  evidence: Split from story 2 at the token-count checkpoint. AD-16 already makes WhatsApp the secondary channel (self-host is Telegram-first because Telegram long-polling needs no public URL), and WhatsApp cannot be exercised at all without a public webhook, a Meta app and a verified number. The Channel port's four operations are fixed by AD-7 and were designed from both platforms together, so this is additive rather than a redesign. Note from the extraction manifest: hermes-agent's adapter is NOT portable for the window logic — it is always inside the window by construction and never chooses between free-form and template.

- source_spec: `_bmad-output/specs/spec-half/stories/2-channel-and-thread.md`
  summary: Runtime processes inbound strictly sequentially across all mains, so the per-main mutex currently buys nothing beyond what the loop already guarantees.
  evidence: Runtime.run awaits _handle inline inside the async for. test_different_mains_do_not_block_each_other proves the registry permits parallelism, but no production code spawns a task. Needs a bounded task group before concurrency is real.

- source_spec: `_bmad-output/specs/spec-half/stories/2-channel-and-thread.md`
  summary: The Telegram poll offset is not persisted, so a restart replays whatever Telegram still holds.
  evidence: At-least-once is now correct within a process (offset committed after the turn) and the turn is idempotent, so redelivery is harmless — but the offset lives only in memory. Persisting it alongside the store would cut redundant replay after a restart.

- source_spec: `_bmad-output/specs/spec-half/stories/2-channel-and-thread.md`
  summary: A multi-chunk send that fails partway leaves earlier chunks delivered, and a retry re-sends them.
  evidence: SendFailed now names which chunk failed, but the retry path resends the whole reply. Needs resume-from-chunk, or an idempotency key per chunk.

- source_spec: `_bmad-output/specs/spec-half/stories/2-channel-and-thread.md`
  summary: Only the inbound half of an exchange is recorded; Half's own reply is never appended to the log.
  evidence: Runtime._pipeline records the main's message and returns the reply, which the channel sends and nobody stores. Any later story that wants conversational history — or the metrics triad — needs both halves.

- source_spec: `_bmad-output/specs/spec-half/stories/2-channel-and-thread.md`
  summary: No linter or type checker configured (ruff, mypy), which is why dead imports and an empty finally block survived to review.
  evidence: Carried over from the earlier CI entry; the CI half is done, lint and type-check remain. Three reviewers each independently reported unused imports.

- source_spec: `_bmad-output/specs/spec-half/stories/3-ingestion-and-secrets.md`
  summary: Build the interactive Gmail OAuth consent flow — authorization URL, code exchange, refresh handling — behind the existing SecretStore port.
  evidence: Split from story 3 at the checkpoint. It needs a Google Cloud project, an OAuth consent screen and a verified app before it can be exercised at all, so it cannot be tested hermetically. Story 3 keeps a thin Gmail fetch that takes a supplied token, which is the telegram_transport.py pattern — a real implementation validating the port while the testable logic stays offline.

- source_spec: `_bmad-output/specs/spec-half/stories/3-ingestion-and-secrets.md`
  summary: Claim derivation — turning captured sources into beliefs — which is the other half of CAP-3.
  evidence: Deferred while writing story 3's spec. Requires the ModelProvider port (AD-19), which is unbuilt and carries its own decisions about prompt shape, cache breakpoints and cost. Pairing a model integration with the safety-critical secret path in one review is the wrong risk combination. Story 3 delivers CAP-13 in full and CAP-3's ingestion half; the independence machinery it builds is what gates admission once derivation exists.

- source_spec: `_bmad-output/specs/spec-half/stories/3-ingestion-and-secrets.md`
  summary: Ingestion is constructed by the composition root but never scheduled — nothing triggers a mailbox walk.
  evidence: build() now returns a Wiring carrying the credential store and a per-main source store, and a Pipeline can be built from it, but no cron, command, or trigger runs one. Belongs with the nightly-pass scheduler (story 9) or an explicit sync command.

- source_spec: `_bmad-output/specs/spec-half/stories/3-ingestion-and-secrets.md`
  summary: The ingestion cursor is returned but never persisted, so every run re-walks from the beginning.
  evidence: Ingested.cursor now advances even when nothing is captured, which fixes the re-fetch-forever case within a run, but nothing writes it down between runs. Idempotency makes that safe but wasteful.

- source_spec: `_bmad-output/specs/spec-half/stories/3-ingestion-and-secrets.md`
  summary: Redaction remains a denylist; the residual risk is now bounded by not retaining bodies rather than eliminated.
  evidence: Option 2 was chosen deliberately (AD-13 amended). A secret Half fails to recognise still passes through the consumer in memory and could be carried into a derived claim once derivation exists. Claim derivation must scrub its own output before persisting it.

- source_spec: `_bmad-output/specs/spec-half/stories/4-retrieval-and-context.md`
  summary: The AD-18 two-channel context builder — `assert` material entering as quotable content and `behave` material as transformed directives whose literal text never appears in a constructed context.
  evidence: Split from story 4 at CHECKPOINT 1 to keep the retrieval spec inside the token ceiling and to give AD-18's byte-wise no-quotation assertion a dedicated review pass. Must land BEFORE story 5, not inside it — AD-18 puts license enforcement at context-construction time, so a license-blind builder written later forces exactly the retrofit AD-10 exists to prevent. Until it lands, story 4's constraint holds: retrieval is wired into the live turn but no retrieved belief text may reach an outbound message.

- source_spec: `_bmad-output/specs/spec-half/stories/4-retrieval-and-context.md`
  summary: `Candidate` is frozen in name only — it carries a `Mapping` belief and a `Mapping` of weights that are ordinary mutable dicts passed straight through.
  evidence: `@dataclass(frozen=True, slots=True)` prevents rebinding the fields, not mutating what they point at. `Candidate.belief` is the dict `db._decode` built and `Candidate.weights` is built fresh per call, so a reranker handed a candidate can rewrite either and the retriever will never notice. Wants `MappingProxyType` at construction, or the honest admission that immutability here is shallow.

- source_spec: `_bmad-output/specs/spec-half/stories/4-retrieval-and-context.md`
  summary: Three different things are called `belief(s)` within one call chain — `Ranked.beliefs` (candidates), `Candidate.belief` (one record), `State.beliefs` (id → record).
  evidence: `ranked.beliefs[0].belief` reads as a typo and is not one. The glossary fixes "belief" as the domain term, so the collision is between a container of ranked candidates and the records themselves. Wants `Ranked.candidates` and `Candidate.record`, which is a rename across the retrieval package and its tests.

- source_spec: `_bmad-output/specs/spec-half/stories/4-retrieval-and-context.md`
  summary: `RerankSource.FAILED` collapses two failures that want different responses — the reranker crashed, versus it returned a set the retriever refused.
  evidence: `Retriever._rerank` catches both a raise from the implementation and the `RetrievalError` from `_accept` (invented, repeated or dropped candidates) into one annotation. A crash is an operational fault; returning a candidate it was never offered is a contract violation and the more alarming of the two. Both are correctly non-fatal, but a caller cannot tell them apart and neither can an operator reading logs.

- source_spec: `_bmad-output/specs/spec-half/stories/4-retrieval-and-context.md`
  summary: The list of derived tables is duplicated across three sites in `half/store/db.py`.
  evidence: `_TABLES` (the drop list), the `DELETE FROM` loop in `rebuild`, and the scan in `is_empty` each enumerate the tables independently, and only `_TABLES` includes `belief_fts`. A table added to `SCHEMA` and to two of the three keeps every test green while leaking rows the log does not describe.

- source_spec: `_bmad-output/specs/spec-half/stories/4-retrieval-and-context.md`
  summary: No lint or type-check gate, so the `# noqa: BLE001` suppressions in `rank.py` and `db.py` are inert decoration.
  evidence: ruff is neither a dev dependency nor a CI step, and mypy is neither configured nor run, so nothing reads those codes. Two deliberate broad `except Exception` blocks — the optional reranker and the injected prefix builder — are argued in comments but unenforced by any tool. Narrows the still-open lint/type half of story 1's "no CI workflow, lint, or type-check configuration" entry rather than replacing it.

- source_spec: `_bmad-output/specs/spec-half/stories/4-retrieval-and-context.md`
  summary: An ordinary turn now costs `state()` → retrieve → `record` → full log fold → whole-database rewrite → `state()`, and `ActorRegistry._hydrate`'s "cheap — SQLite open plus a snapshot read" comment is no longer accurate.
  evidence: **The rebuild-per-append predates this story** — `Store.append` called `self.rebuild()` at story 1's baseline (b486c328), and that O(n)-per-append cost is already filed above from story 1. What story 4 added on top is two full `State` reads per turn (one for the idempotency check, one for `known_strands`) plus a `loops()` query, each folding or scanning the whole belief set. A single hydrated `State` threaded through the turn would remove both extra reads without touching the pre-existing rebuild question. The stale hydration comment should be corrected whenever either is addressed.

- source_spec: `_bmad-output/specs/spec-half/stories/4b-two-channel-context.md`
  summary: Non-Latin retrieval is broken in two distinct ways — a story-4 query-builder regression that destroys precision for combining-mark scripts, and a genuine gap for scriptio-continua scripts.
  evidence: **Corrected diagnosis (2026-09-01).** An earlier version of this entry blamed FTS5's `unicode61` and called for a schema change; that was wrong. Verified directly against SQLite 3.53.1: `unicode61` does split combining-mark scripts into bare consonants, but the index and the query split identically, so a phrase query still resolves correctly — `MATCH '"रात"'` returns only the night document, and `MATCH '"यात्रा"'` only the travel one. The precision collapse comes from Half's own query builder: story 4 replaced phrase quoting with term-OR over `half.text.words` output, so a Devanagari query is shattered into single consonants and OR-joined, and `रात` ("night") matches a belief about travel. Fix: quote each source word as a phrase, keeping OR between words. No schema change needed for Devanagari, Arabic or Thai *diacritics*. Separately and genuinely: scriptio-continua scripts have no spaces, so `unicode61` makes a whole Japanese or Thai sentence one token and `転職` matches nothing; `trigram` fixes Thai but fails on two-character CJK words, which are very common. That half needs n-gramming at index and query time — the claude-obsidian extraction row (`_script_runs`, `_cjk_ngrams`, its schema-2 CJK n-gram tokenizer) is the reference, and it does change indexed content, so it needs the schema version bump that story 4's `_discard_if_stale` already supports.

- source_spec: `_bmad-output/specs/spec-half/stories/4b-two-channel-context.md`
  summary: `assert`-licensed beliefs contribute content but never a directive, so a belief Half may state cannot also shape how it behaves. The ladder semantics were never decided, only implemented.
  evidence: `half/context/build.py::_item` returns exactly one channel item per candidate, and the matrix row "Mixed set — each lands in exactly one channel" was read as forbidding a second. But a rung is a *permission*, and `assert` is strictly stronger than `behave`: a belief Half may quote is certainly one it may act on quietly. As shipped, promoting a belief to `assert` silently removes its topic from the directives, which is a demotion wearing a promotion's clothes. Wants a decision — cumulative rungs, or one channel each — recorded in the glossary's license-ladder entry rather than inferred from a matrix row about disjointness.

- source_spec: `_bmad-output/specs/spec-half/stories/4b-two-channel-context.md`
  summary: `Content`, `Topic` and `Context` are publicly constructible, so "nothing downstream may re-admit material the builder excluded" is a convention rather than a construction.
  evidence: `Context(now="x", content=(Content(id="b_1", claim=<any behave claim>),))` type-checks, runs, and produces a context that never passed the fragment guard — `build` is the only thing that applies it. Immutability stops a context being *edited* after the scan; it does not stop a second one being assembled from scratch. Closing it means a private constructor plus a builder-only factory, or moving the guard into `Context.__post_init__` where every construction pays it. Not urgent while `build` is the only caller, and `tests/test_entrypoint.py`'s one-caller shape is the precedent for asserting that statically instead.

- source_spec: `_bmad-output/specs/spec-half/stories/4b-two-channel-context.md`
  summary: `respond` reads `quotable()[0]` and discards the receipt id, so the one claim Half states arrives without the citation the constitution requires.
  evidence: *"Half is structurally incapable of asserting anything about the main without a citation into its own evidence"* (constitution, Truth). `Content` carries its belief id precisely so a reply can cite it, and the stub throws it away — along with every content item after the first. Harmless while the responder is a deterministic placeholder and no model is called (AD-19), but story 5 must not inherit the shape: the model call should receive the whole content channel with ids attached, not one bare string.

- source_spec: `_bmad-output/specs/spec-half/stories/4b-two-channel-context.md`
  summary: `half/actor/runtime.py` imports `half.context.build` directly rather than the package, bypassing the curated `__all__`.
  evidence: `from half.context.build import build as build_context` reaches past `half/context/__init__.py`, which exists to say what the package offers. Cosmetic today; it matters when the package grows a second entry point and callers are split between the two spellings.

- source_spec: `_bmad-output/specs/spec-half/stories/4b-two-channel-context.md`
  summary: `now` is rendered into the context unvalidated, straight from `inbound.t`.
  evidence: `Context.render` emits `now: {self.now}` with no check that it is an ISO-8601 stamp, while `half/retrieval/rank.py` raises `RetrievalError` for a `now` it cannot parse and `half/store/records.py` validates `t` at decode. The context builder is the one place on the turn that accepts whatever the adapter wrote. Sanitization stops it forging a line, so this is a correctness and consistency gap rather than a safety one — but a model reading `now: garbage` is being told the time by something that never looked.

- source_spec: `_bmad-output/specs/spec-half/stories/4b-two-channel-context.md`
  summary: `tests/test_context.py::context_of` imports `Retriever` inside the function body.
  evidence: A function-local import with no circularity to break and no cost to avoid; every other import in that file is at module scope. Trivial, and left alone only because moving it touches a file under active review.

- source_spec: `_bmad-output/specs/spec-half/stories/4b-two-channel-context.md`
  summary: The AD-18 fragment guard uses an adjacent-word-pair floor, so an `assert` claim sharing any two consecutive words with a withheld claim is dropped from content — including purely grammatical pairs.
  evidence: Verified against a live build. With a withheld claim "has been avoiding his brother since the funeral", a licensed claim "has been running every morning before work" is dropped on the shared pair "has been" alone, as is one sharing only "the funeral". The floor cannot be one word — a single shared word is exactly what a directive is, so a one-word floor empties the directive channel and lands on AD-18's second named failure. Two is the safe choice and fails in the right direction, but "has been", "going to" and "in the" are among the commonest English bigrams, so the content channel may be quieter in practice than intended. Refinements all have costs: a distinctiveness test needs per-language frequency data, which conflicts with shipping world-wide; a three-word floor widens the leak. Revisit with real conversation data once story 5 gives the content channel a consumer — it is one constant and one Spec Change Log entry away.

- source_spec: `_bmad-output/specs/spec-half/stories/4c-script-neutral-retrieval.md`
  summary: Word-boundary tokenization cannot match a word that carries an attached clitic or inflection, so recall is incomplete for Arabic, Hebrew, Turkish, Finnish and Korean particles.
  evidence: Verified after 4c landed. A belief claiming "يخطط لرحلة الشهر المقبل" is not retrieved by the query "رحلة", because Arabic attaches the preposition and the indexed token is "لرحلة". Hebrew "טיול" retrieves correctly only because it happens to appear bare. `tests/test_scripts.py` covers each script with a bare surface form, which is honest for what the tokenizer promises but leaves this untested. This is morphology, not tokenization — a real fix needs stemming or affix stripping, which is per-language and cuts against 4c's own constraint that Half must not need a language list. Options when it matters: prefix-matching the FTS query (`token*`) for scripts with productive clitics, or accepting the recall gap and leaning on the other three ranking factors. Not urgent while no such corpus is ingested; revisit with real non-Latin usage.

- source_spec: `_bmad-output/specs/spec-half/stories/5a-license-ladder.md`
  summary: CAP-10's trust balance and the unsaid and unasked queues — insights held above the current license with release conditions, and clarifying questions paid for by stakes rather than favours.
  evidence: Split from story 5 at the spec checkpoint, with Vidit choosing a three-way slice. Needs 5a's ladder to exist first, since a queue holds what the ladder refuses. Carries the gbrain voice-gate manifest row if tone judging lands with delivery. Queue depth is itself a signal (glossary), so the queues must be inspectable, and "an unspent balance is a defect" means the trust balance needs a test that spending happens, not merely that it is tracked.

- source_spec: `_bmad-output/specs/spec-half/stories/5a-license-ladder.md`
  summary: CAP-10's irreversibility interrupt — Half breaks through unprompted only when waiting would destroy an option. **The per-loop nagging bound moved into story 10 (2026-09-02)**, because it is that story's selection rule rather than a separate feature: a morning surface that picks the highest-ranked loop each day raises a years-long loop every morning, which is the definition of nagging.
  evidence: Split from story 5 and deliberately sequenced AFTER story 8, because "nagging" is defined against a loop's natural timescale and loop objects carrying state and timescale are CAP-6 (story 8). Today's fold has a `loops` dict populated only by `loop_transition` ops, with no timescale field, so building the bound now would mean inventing story 8's schema early or shipping a rule that cannot compute. Consumes the gbrain `src/core/calibration/nudge.ts` manifest row (14-day per-pattern cooldown and its feedback-loop prevention). Its consumers are stories 9 and 10, both of which also follow story 8.

- source_spec: `_bmad-output/specs/spec-half/stories/6a-crisis-switch-and-moment.md`
  summary: LAUNCH GATE, not deferrable work — clinical review of the crisis protocol by a qualified clinician, which the companion marks non-negotiable.
  evidence: The crisis companion's build requirement 6 is "Clinical review before launch. Non-negotiable." No test suite, red-team pass or reviewer round substitutes for it, and a green suite must never be read as satisfying it. Recorded here so it cannot be lost: Half must not ship to a real main until a qualified clinician has reviewed the entered mode, the templated responses, the never-list and the red-team results. Also unresolved and clinically relevant: minors, and jurisdictions with mandatory reporting duties (companion open question 4) — v1 has no answer and shipping without one is a decision to make deliberately, not by omission.

- source_spec: `_bmad-output/specs/spec-half/stories/6a-crisis-switch-and-moment.md`
  summary: The warm handoff — a confirmed contact list built cold, two or three choices offered in the moment, prefilled one-tap drafts, and a crisis-line directory versioned as refreshable data.
  evidence: Split from story 6 at the spec checkpoint. The evidence weight here is high: warm handoff more than triples first-appointment attendance, and the main's own therapist is the highest-value contact because that connection is precisely what documented chatbot failures missed. `Channel.draft_link` already exists from story 2, so the mechanism is present; what is missing is the list and the directory. The list must be built cold months earlier by one ordinary question ("who's the person you'd call first if something went wrong?"), never an emergency-contacts form, and confirmed rather than inferred — so it couples to story 11's question engine. The directory must be refreshable without a release (build requirement 5), and "nearby therapist, worldwide" is explicitly out of scope as unmaintainable.

- source_spec: `_bmad-output/specs/spec-half/stories/6a-crisis-switch-and-moment.md`
  summary: Aftercare — the minimum thirty-day period, gradual rather than all-at-once license restore, asking before the mirror resumes, Caring Contacts, and holding a clinician-authored safety plan.
  evidence: Split from story 6. 5a's durable ceiling is the mechanism and 6a lowers it on entry, so the cap is real from the first crisis turn; what 6c owns is coming back. Restore must be gradual and Half must ask before the mirror resumes rather than resuming silently. Never gated by tier and continues for a lapsed or free main. Caring Contacts are periodic messages of unconditional care with no demand attached over 1-2 years, which needs AD-9's due-time queue from story 9. Half must never author a safety plan — that is clinical — but holds a Stanley-Brown plan made with a professional and can produce it instantly, which is the whole point since a plan in a drawer is useless at 3am. Companion open questions still unanswered: who decides the mode is over, and what 1-2 years means for a lapsed or free main.

- source_spec: `_bmad-output/specs/spec-half/stories/6a-crisis-switch-and-moment.md`
  summary: TOP CLINICAL-REVIEW ITEM — crisis detection is a phrase table and is therefore English-only and blind to the vocabulary people actually use; a model classifier that only ever decides, never writes, is the fix.
  evidence: Verified against the shipped detector. `Tier.NONE` for "kms", "unalive myself", "im sucidal", "s u i c i d a l", "i don't want to be here", "i've written a note", "i won't be here tomorrow", "i'm done." — and for every non-English phrasing, since the tables are English-only. A phrase table errs toward silence by construction, which inverts the companion's asymmetry principle: it fires only on what someone thought to write down, and the set of ways a person says this is not enumerable. Story 6a's chosen fix separates asking from entering so the cheap action has broad reach, but recall is still bounded by the table. The real repair is AD-19's model port used as a CLASSIFIER — deciding entry only, with replies staying 100% templated so no model ever writes a word a main in crisis reads, and failing TOWARD entering when the model is unavailable or uncertain. This is the single largest gap the clinical reviewer must be told about.

- source_spec: `_bmad-output/specs/spec-half/stories/6b-warm-handoff.md`
  summary: LAUNCH GATE — every crisis-line entry in `data/crisis-lines.json` was written by a language model from memory and must be replaced with operator-verified data before any main sees it.
  evidence: 19 regions, each with a name and a `reach`. The file carries a provisional note and the CI gate states explicitly that it does not cover the data, but neither prevents it shipping. This is the most dangerous artifact in the repository: a wrong number in a crisis is worse than the generic line, because it costs a call at the worst possible moment, and a plausible-looking wrong number is exactly what a model produces from memory. A second model reading the file and recognising the entries is NOT verification — it is the same source agreeing with itself. Helpline numbers also change, and any model has a knowledge cutoff. Required before launch: every entry checked against the operator's own sources, a named owner for the file, and a refresh cadence. The loading path already degrades to the generic line, so shipping with an EMPTY directory is strictly safer than shipping with unverified entries.

- source_spec: `_bmad-output/specs/spec-half/stories/6b-warm-handoff.md`
  summary: UPDATE to the launch gate above — the unverified directory is now refused in code, not only in a note. `data/crisis-lines.json` carries `reviewed: false` and Half names no line at all until a qualified reviewer flips it.
  evidence: Review round 1 asked for the file's own review state to be parsed and surfaced so an unreviewed directory degrades to the generic line rather than shipping silently as the default. `Directory.reviewed` defaults to false and is read strictly — anything that is not an explicit `true` is not a review, for the reason `known_to_main` is read strictly: the field grants a permission, and the permission here is to put an unverified phone number in front of somebody in crisis. `listings_for` returns nothing while it is false, and `tests/test_directory.py::test_the_shipped_directory_names_nothing_until_it_is_reviewed` fails the build if the flag is flipped without updating the test, so signing it off is a deliberate act with a name on it. The entries still parse and the version is still reported, so a reviewer can inspect the file and a log can say which version was declined. What has NOT changed: the entries are still model-written and still unverified, a second model reading them is still not verification, and the required work is still every entry checked against the operator's own sources, a named owner, and a refresh cadence.

- source_spec: `_bmad-output/specs/spec-half/stories/6b-warm-handoff.md`
  summary: CLINICAL-REVIEW ITEM — the whole handoff repeats byte-identically on every turn the crisis mode is open, and whether that is presence or pressure is a clinical judgement.
  evidence: Nothing exits the mode, so every later turn resolves to the held plan and re-serves the same two or three doors. The safety argument for repeating is that a door offered once is a door for somebody who was reading carefully, which is nobody here. The argument against is the companion's own tone correction: rushing to fix reads as MINIMISING and is counterproductive, and re-serving a list of numbers after every message is a way of hurrying somebody. The alternatives — offer once and then only on request, thin the offer after the third turn, repeat the people but not the lines — each encode a clinical judgement, and picking one in a code comment would be answering a clinical question by accident, exactly as a mode-exit timeout would. `tests/test_handoff.py` pins invariance to the phrasing (the safety property) and says in its own docstring that the repetition itself is unsettled. Goes to the reviewer with the wording.

- source_spec: `_bmad-output/specs/spec-half/stories/6b-warm-handoff.md`
  summary: A crisis line is rendered as a string the main copies by hand, so the highest-volume door asks the most of the person least able to give it.
  evidence: A confirmed contact gets a one-tap prefilled draft via `Channel.draft_link`; a crisis line gets `"Samaritans - 116 123 . 24/7, free"` and the main types the digits at three in the morning. A tappable form is mechanically easy (a `tel:` URI, or a prefilled message where the service accepts one), but writing a person's opening words to a crisis service is a different act from writing them to their brother, and the story's Ask-First rule covers adding a contact channel beyond a link the main taps. Deferred to the clinical reviewer with the template wording rather than decided in review; recorded here so the asymmetry is not mistaken for an oversight.

- source_spec: `_bmad-output/specs/spec-half/stories/6b-warm-handoff.md`
  summary: Four shipped regions hold exactly one crisis line, so a main there who has confirmed nobody gets 6a's generic wording rather than a door.
  evidence: `br`, `de`, `nl` and `sg`. Review round 1 found three of them meeting the two-line rule by listing one organisation's two phone numbers, which reads as two doors and is one; the duplicates were removed rather than replaced, because padding the file from memory increases the surface without increasing the truth. One door is not a choice — the companion is explicit that the closest person is sometimes the wrong one and that control matters most here — so the handoff refuses a lone door and the honest generic sentence stands. The set is pinned in `tests/test_directory.py::THIN_REGIONS` so it can only shrink deliberately. Correct fix: the operator-verified data pass named in the launch gate above adds real second organisations.


- source_spec: `_bmad-output/specs/spec-half/stories/6c-aftercare.md`
  summary: Caring Contacts — brief periodic messages of unconditional care with no demand attached, over one to two years, which the companion calls the core of aftercare.
  evidence: Split from 6c because it needs a scheduler and none exists anywhere in the product: AD-9's due-time queue arrives with story 9, and story 10 owns the at-most-one-unprompted-message-a-day rule these must respect. The rest of aftercare deliberately needs no scheduler — the restore is evaluated on the main's next inbound turn, which is better than pushing because the main is present. Caring Contacts cannot use that trick, since their whole value is arriving when the main has not written. Evidence is strong: meta-analysis shows a protective effect against suicide attempts, they are cost-effective and appear in multiple clinical guidelines, and the SPRING RCT (n=849) is testing one-way versus two-way — track it before choosing. They work BECAUSE they carry no agenda, which collides with everything else Half does, so they must be built inside the mirror-off period rather than beside it. Companion open question 2 is still unanswered: what one-to-two years means for a lapsed or free main, given aftercare is never tier-gated.

- source_spec: `_bmad-output/specs/spec-half/stories/9a-due-time-scheduler.md`
  summary: The `ModelProvider` port (AD-19) with prompt-cache breakpoints first-class, per-actor tier (AD-20), and Batch API submission (AD-9) — the single most-deferred item in the project.
  evidence: Stories 3, 4, 4b, 5a and 6a each explicitly deferred work to this port. It is also the first thing that breaks a property every story has held so far: the suite is entirely hermetic and offline, and the test strategy for a network-calling port is its own problem rather than a detail of whatever story happens to need it first. AD-19 requires cache breakpoints to be first-class and never hidden, because the free-tier cost model rests on caching the stable prefix (system prompt, constitution, belief fold). AD-20 puts the tier on the actor. AD-9 requires batch submission, roughly 8pm local for a 7am delivery with about eleven hours of slack. Vidit chose to build it after the scheduler and before consolidation. Note the crisis constraint from 6a: a model may CLASSIFY but must never author a word a main in crisis reads, and it must fail toward entering the mode.

- source_spec: `_bmad-output/specs/spec-half/stories/9a-due-time-scheduler.md`
  summary: Consolidation itself (CAP-7) — promoting episodic material to durable claims, decaying unused salience, and minting tensions between the stated and revealed ledgers with computable widening.
  evidence: Split from story 9 at the spec checkpoint; needs both the scheduler and the model port. CAP-7 is explicit that comparison is bounded to new or changed entries against the loop set and against beliefs sharing a subject, gated by a cheap relevance filter BEFORE any model comparison, never all-pairs — the cost budget is the design, not a tuning parameter. Carries three open manifest rows: graphiti's edge resolution returning `(resolved, invalidated, new)`, its episodic-vs-entity nodes and saga summaries, and honcho's surprisal-based sampling to target reasoning at anomalous observations; HippoRAG's `DSPyFilter` is the model of the cheap pre-filter. Story 8's loops are the ranking input and now exist; salience decay was deferred here from story 4.

- source_spec: `_bmad-output/specs/spec-half/stories/6d-crisis-classifier.md`
  summary: LAUNCH BLOCKER — nothing tells a main that their messages now leave the machine, and there is no way to decline classification egress short of removing the key.
  evidence: 6d makes the product's first content egress: a main's message goes to a model provider on most turns. The companion's standing rule is that the main must know what Half holds, and the first egress deserves at least that. It is a launch blocker rather than deferred work because the fix is a sentence a main reads, and every such sentence is Ask-First plus clinical review — writing one here would be inventing reviewed wording. Needed before launch: an onboarding line, and a per-main flag that declines classification without removing every other model capability with it.

- source_spec: `_bmad-output/specs/spec-half/stories/6d-crisis-classifier.md`
  summary: `another_at_risk` is detected by the classifier and then acts on nothing, and 6c's `stop_asking` and plan-request vocabularies carry the same English-only asymmetry 6d just fixed for affirmatives.
  evidence: The classifier can tell that a message is about somebody else at risk, and the mapping sends that turn to the ordinary pipeline. 6a's third-party path surfaces a resource; the vigilance tiers it defines have no producer and were recorded as unimplemented. The alternatives are a model authoring a sentence about a third party (forbidden) or building the vigilance surface (6a's deferred work), so it goes to the clinical reviewer rather than being decided here. Separately: 6d widened `AFFIRMATIVE_SOURCE` from 34 to 104 phrases so a main can confirm in their own language, but `stop_asking` (6c) and the plan-request phrases (6c) are still English-only, so a non-English main can enter the mode and then cannot stop being asked or ask for their own safety plan.

- source_spec: `_bmad-output/specs/spec-half/stories/9c-tension-states.md`
  summary: Tension minting — the bounded comparison that finds two entries that disagree, gated by a cheap relevance filter before any model call.
  evidence: Split from 9c so the vocabulary and the model-comparison cost logic do not land in one review, which is the combination the last four stories each needed splitting to avoid. CAP-7 is explicit that the cost bound is the design rather than a tuning parameter: comparison is bounded to new or changed entries against the loop set and against beliefs sharing a subject, gated by a cheap relevance filter BEFORE any model comparison, never all-pairs. The reference for the filter is HippoRAG's `DSPyFilter`, which prunes candidates before the expensive stage; story 4 already took that shape for the reranker and recorded what not to copy. Also carries graphiti's edge resolution returning `(resolved, invalidated, new)` and honcho's surprisal-based sampling to target reasoning at anomalous observations. The model port and its per-pass budget now exist, and the classifier shows the shape: a model that decides, with the caller interpreting failure. Note CAP-7's decay half is already delivered — story 4's salience is computed with a ninety-day half-life, so a decay pass would mean storing a counter for a pass to mutate, reintroducing the AD-30 violation story 4 avoided.

- source_spec: `_bmad-output/specs/spec-half/stories/10-morning-surface.md`
  summary: LAUNCH BLOCKER — the morning surface sends machine-facing text. What reaches the wire today is `now: <stamp>` followed by `content[<id>]: <claim>`, which is the context builder's rendering rather than a sentence a person should receive.
  evidence: Story 10 deliberately builds no composition — it chooses *what* to say and proves it may be said, and the frozen block forbids a model call. The implementer flagged this rather than papering over it, and it is right: the story satisfies its own text and the result is not sendable. Nothing may reach a real main from this path until a composition story turns a permitted candidate into a sentence. Note the constraint that composition inherits: `assert` material may be quoted, `behave` material may shape the sentence and never appear in it (AD-18), and the whole reply is still subject to the crisis never-list if the main is in the mode.

- source_spec: `_bmad-output/specs/spec-half/stories/10-morning-surface.md`
  summary: A candidate that names no loop can never be surfaced, and a loop with no timescale can never be raised even once.
  evidence: Both are the implementer's conservative readings of a spec that does not settle them, and both were flagged rather than hidden. CAP-8 says content traces to "a tension, loop transition, or ingested item" — it does not require a loop — so a tension between two beliefs that sit on no loop is currently unsurfaceable for ever. The argument for the conservative choice is real: the per-loop nagging bound is the only frequency guard this story has, and a non-loop candidate would be bounded only by one-a-day plus 9c's rule that a transition to the state already held is refused — which verification-gap showed can still churn widening/closing/widening. Deciding this properly means either giving non-loop candidates their own bound or accepting churn as bounded by one-a-day. Separately, a loop opened with no timescale is permanently unsurfaceable until someone rescales it, and nothing tells the main.

- source_spec: `_bmad-output/specs/spec-half/stories/5b-trust-and-the-unasked.md`
  summary: The unsaid queue — insights Half holds because it lacks the license to deliver them, each with a release condition, where queue depth is itself a signal.
  evidence: Split from 5b because the two queues share a name and little else. The unasked queue is gated by stakes and by the favour rule, and is what story 11's question engine spends; the unsaid queue is gated by the license ladder and by release conditions — trust level, the main raising the topic, day of week, expiry — and is what 5a's ladder refuses to speak. Building both in one story would put the currency and the license machinery in one review. The glossary's note that queue depth is itself a signal means the queue must be inspectable rather than purely internal, and CAP-10's "insights above the current license are queued with release conditions and are never placed in the model's context as quotable content" ties its release path to AD-18's two-channel split, which 4b already enforces.

- source_spec: `_bmad-output/specs/spec-half/stories/5b-trust-and-the-unasked.md`
  summary: A preference has no wanting behind it, so `stakes` refuses it as `no-wanting` and Half will never ask about one — CAP-4 names preferences explicitly, so two of its four categories are unaskable. Needs its own cost measure and its own story.
  evidence: Verified against the built rule at `half/trust/stakes.py`. With a live `Loop` fixture: a preference with `loop=None` returns `worth_asking=False reason=no-wanting`; a days-routine returns `below-the-bar`; only months- and years-loops return `worth_asking=True`. CAP-4's intent reads "plans, ambitions, preferences, routines". The days-routine refusal is correct on the merits (a routine that moves in days self-corrects inside a day, so being wrong costs less than the interruption). The preference refusal is not — a preference is acted on repeatedly, so being wrong is wrong every time, but the loop period is the wrong yardstick for it and no second yardstick exists. Deliberately not patched inside 5b: inventing a second cost measure under a frozen Never list is the "individually-correct fixes composing into a new defect" shape this project has hit repeatedly.

- source_spec: `_bmad-output/specs/spec-half/stories/5b-trust-and-the-unasked.md`
  summary: A failed send still earns a favour, because the morning surface claims the day before it sends, so Half can over-count by one favour per failed send.
  evidence: Reported by the 5b implementer and consistent with story 10's design: `MorningSurface` calls `_claim` then `channel.send`, so a send that raises leaves a durable `touch` record with `sent=True`, which `half/trust/balance.py::delivered` counts as a delivered favour. The 5b matrix row "the send failed -> nothing is earned" is therefore not true of the log. Bounded: at most one favour per failed send, and the direction lets Half ask slightly more than it earned. Fixing it needs a durable record of send failure, which changes story 10's op and surface, and turns on an Ask-First question ("what counts as a delivered favour") that 5b's frozen block does not answer.

- source_spec: `_bmad-output/specs/spec-half/stories/5b-trust-and-the-unasked.md`
  summary: Nothing records that a question was *answered*, only that it was put, so the unasked queue cannot tell "already asked and answered" from "asked and ignored" and no sixth gate against re-asking can be written yet.
  evidence: Raised in review (a question stays askable and is re-asked whenever a new favour lands) and deliberately not fixed. The `asked` op records that a question was put to the main; no op anywhere records the reply. A gate on `asked` records alone would therefore not mean "do not nag" but "never ask twice whatever happened", permanently silencing a question the main merely ignored. Story 6c hit the identical problem for aftercare and solved it by making the answer its own durable state (`AFTERCARE_ASKED` vs `AFTERCARE_DECLINED`/`AGREED`), under a comment that silence is not consent. Questions need the same op, and receiving an answer belongs to story 11. Until then re-asking is bounded by the currency: every attempt costs a favour. Recorded as a named boundary in `half/trust/unasked.py`'s module docstring.

- source_spec: `_bmad-output/specs/spec-half/stories/11-the-bought-question.md`
  summary: A turn's question can be funded by a favour from a morning that was claimed seconds earlier and not yet sent, because story 10 claims the day before sending.
  evidence: Narrower instance of the existing "a failed send still earns a favour" entry. Story 10's `_claim` writes `touch.spoke()`, which sets `sent=True` and marks the day, and `half/trust/balance.py::delivered` counts exactly that — so the favour exists from the claim, not from the delivery. Story 11's review-loop-1 fix closes the case where a run funds *its own* question (the turn writes no record `delivered` counts, asserted structurally), but a morning tick landing between a turn's offer and its spend still contributes a claimed-but-unsent favour. Reachable only when a pre-dawn tick coincides with a live turn. Closing it needs the log to record delivery, which is a new field on a closed op and therefore Ask-First.

- source_spec: `_bmad-output/specs/spec-half/stories/11-the-bought-question.md`
  summary: An undelivered ask can still be retired by an unrelated reply inside its own day.
  evidence: Reported by the implementer and deliberately not fixed. The `asked` record is written before the send (5b's spend contract), so a question whose send then fails is recorded as put. Review loop 1 bounded retirement to the most recently asked question and to `ANSWERS_WITHIN_DAYS`, which turns the original "silenced for ever by any later message on any subject" into "silenced for one day by a message sent that day". Closing it entirely requires the log to record delivery — a new field on a closed op, Ask-First. Stated in `half/questions/answered.py`'s docstring rather than hidden.

- source_spec: `_bmad-output/specs/spec-half/stories/12-correction.md`
  summary: LAUNCH BLOCKER — the correction phrase tables need native-speaker review; coverage is ~35x richer in English than in the thinnest scripts, and Thai carries no rows at all for two of the four meanings.
  evidence: Measured against the shipped tables after the review round. Row counts by script (never_true / changed / wrong / erase): Latin 36/34/40/25, CJK-JP 5/5/7/5, Arabic 3/3/3/2, Cyrillic 3/3/3/2, Devanagari 3/3/3/2, Hangul 2/2/3/2, Hebrew 1/1/1/1, **Thai 0/0/2/1**. So a Thai main cannot express "that was never true" or "that changed" through the offline table at all, and a Hebrew main has exactly one phrasing per meaning. Verified misses in common phrasings: Chinese `删掉那个` ("delete that"), Chinese `那不对`, Thai `ไม่ใช่`, Japanese `それは削除して`. The review round also deliberately removed rows that were genuine false-positive sources (`不对`, `違います`, `ไม่ถูก`, `그거 삭제해`), trading recall for safety — the right trade, but it widened the asymmetry. The classifier covers the gaps only while a provider is reachable; with it down, recognition quality varies by language by more than an order of magnitude. Nobody on this project can validate these tables, and adding rows unvalidated is how the false positives arose. Same class of blocker as the operator-verified crisis-line data.

- source_spec: `_bmad-output/specs/spec-half/stories/12-correction.md`
  summary: Reported speech is still read as a correction — "quoting my sister: you were wrong about that" recognises as never_true.
  evidence: Verified against the shipped tree after the review round, which fixed the other reported-speech reproduction ("he told me 'thats wrong' but i disagreed" now returns None). The remaining case survives because the clause-based matcher sees "you were wrong about that", which in isolation genuinely is a correction — distinguishing it requires understanding attribution, which is the classifier's job and not a table's. Bounded by the aim's relevance floor and, for erasure, by the confirmation requirement added in review. Worth revisiting when the classifier's label set is next opened.

- source_spec: `_bmad-output/specs/spec-half/stories/13a-the-morning-in-words.md`
  summary: Nothing checks that a composed message is grounded in what Half was allowed to say, so an invented claim reaches the wire past every gate.
  evidence: The judge carries four rules and none is about provenance; the tripwire covers only *withheld* wordings, so it catches a `behave` claim being quoted but not a claim that was never in the ledger at all. Deliberately not built in 13a: a groundedness check inside a story whose Never list is frozen is the shape that has produced this project's composition defects. It matters more than an ordinary model risk here because the constitution says *assert only with receipts*, and `quotable_block`'s docstring cites exactly that as the reason belief ids are dropped from the prompt. Recorded in `half/voice/gate.py` with three rejected alternatives weighed. Genuinely a product decision: it may be an accepted cost of using a model at all, but it must be decided rather than discovered.

- source_spec: `_bmad-output/specs/spec-half/stories/13a-the-morning-in-words.md`
  summary: The periodic-report alarm is suppressed at every hundredth consultation, in three places; 13a fixed one.
  evidence: `_report`'s `if consulted % REPORT_EVERY == 0: flush() elif alarming: flush(alarming=True)` makes the branches mutually exclusive, so at 100, 200, ... a wholly failing consultation reports at `info` rather than `error`. Identical code exists in `half/crisis/classifier.py` and `half/correction/candidate.py`; 13a fixed `half/voice/gate.py` only, because the crisis path is Ask-First and correction was outside its scope. One bug now living in three copies is the strongest argument yet for the `half/model/consult.py` extraction below.

- source_spec: `_bmad-output/specs/spec-half/stories/13a-the-morning-in-words.md`
  summary: The bound/cap/breaker/tally consultation shape now exists in three copies, plus a third copy of the provider-building loop in the composition root.
  evidence: `half/crisis/classifier.py`, `half/correction/candidate.py` and `half/voice/gate.py` each carry ~200 lines of the same machinery, and story 6d's own corrections (denylist to allowlist, `raised` split from `unreadable`) would now have to be made three times — as the `_report` bug above demonstrates concretely. The 13a implementer recommends extracting `half/model/consult.py` with policy injected, **conditional on** (a) crisis behaviour coming out byte-identical, asserted rather than reviewed, and (b) the shared module holding no labels, no instructions and no numbers — otherwise `test_crisis_golden.py`'s digest pin becomes a pin on a base class and means nothing. `PER_CALL_MICRO_USD` and `PER_PASS_MICRO_USD` are already byte-identical across all three despite a docstring claiming the numbers differ by situation. Needs its own story; the crisis path is Ask-First.

- source_spec: `_bmad-output/specs/spec-half/stories/13b-the-turn-in-words.md`
  summary: `respond` promises the reply never echoes the main's own words, and that promise is asked for in the prompt rather than enforced.
  evidence: Two implementers independently refused the instruction to add the turn's inbound text to the withheld set, and both were right. `half.voice.leak`'s unit is the adjacent word pair, so an ordinary reply reusing two consecutive words of the main's message — two words is a preposition and an article — is refused as a leak, logged at `error` for an operator to read as *AD-18 has broken*, and downgraded to the claim alone. Driven: `hidden | fragments(inbound.text)` fails fourteen cases. What holds today is the instruction forbidding the model to repeat the language-sample block, plus a pinned case asserting that a reply sharing a word pair with the main's message goes out with the tripwire quiet — so a later story that wants this enforced has to come here and change that case deliberately. Enforcing it properly needs a unit other than the adjacent word pair for this one comparison.

- source_spec: `_bmad-output/specs/spec-half/stories/13b-the-turn-in-words.md`
  summary: `cap1_turn`'s CI floor is coupled to a whole file, so every case added to `tests/test_turn_words.py` silently owes a workflow edit.
  evidence: The file carries a module-wide `pytestmark`, and the gate is margin-zero by design (every case in it is the only one asserting its rule). The two are individually correct and compose into a floor that drifts: during story 13b's review it fell out of step four times. The comment now tells a reader to re-read the number off a collection rather than reason about it, which is a mitigation rather than a fix. The structural options — marking per case, as `test_unasked.py` does for `cap4_favour`, or a gate that reads its own floor — are a decision nobody has taken.

- source_spec: `_bmad-output/specs/spec-half/stories/9d-minting-tensions.md`
  summary: Every production belief carries `subject="self"`, so CAP-7's "beliefs sharing a subject" comparison set is the entire stated ledger and does no bounding at all.
  evidence: `half/actor/runtime.py:700` is the only production writer of a belief subject and hardcodes `"self"`; `half/context/build.py:418` already records the fact in prose, and ingestion's belief half is unbuilt (its `subject=` uses are email headers, unrelated). Measured during story 9d: 61 beliefs, one changed, 60 couples — so comparisons scale with `|changed| x N`. This is not all-pairs and CAP-7's actual criterion still holds, because the cheap filter and the per-main budget carry the cost (60 -> 30 -> 24 judgements), but the second comparison set contributes nothing. Fixing it means deriving a real subject at admission, which is a story-3 change to what the turn path records; 9d's Ask-First list deliberately forbade inventing a third comparison set instead.

- source_spec: `_bmad-output/specs/spec-half/stories/9d-minting-tensions.md`
  summary: The cheap filter refuses any pair on the same named ledger, so a stated-vs-stated tension can never be minted.
  evidence: Flagged by the implementer as a behavioural narrowing the spec does not literally require. CAP-7 mandates "a cheap relevance filter" without specifying it, and the glossary frames the mirror as *the gap between the two ledgers*, so the rule is well-aimed — and it is the sharpest of the three, without which the filter would reject almost nothing given the degenerate subject set above. But two things a main has *said* at different times can disagree where neither is wrong, which is the definition of a tension, and that class is currently unmintable by construction. Worth deciding rather than inheriting.

- source_spec: `_bmad-output/specs/spec-half/stories/9d-minting-tensions.md`
  summary: A minted tension does not become a morning candidate, and a pass that mints reports `quiet`.
  evidence: `PassResult.candidates` carries only what *moved*, and `quiet` means "nothing moved and nothing failed". A `fresh` tension evaluated at zero elapsed time reports `unchanged`, so folding minting into either would change what two existing CAP-8 sentences mean. Documented on the field by the implementer rather than changed silently. No production reader of `PassResult.quiet` exists today (`half/schedule/tick.py:408` reads `TickResult.quiet`), so the cost is latent — but the first surface that reads it will read a minting pass as a quiet one.

- source_spec: `_bmad-output/specs/spec-half/stories/14-one-consultation.md`
  summary: An infinite (or NaN) construction bound is accepted by all three consultations, so `asyncio.timeout` never fires and a main waits on a hung provider through the guard written to prevent that.
  evidence: All three constructors validate with `bound_seconds <= 0`, and `float('inf') <= 0` is `False`; NaN likewise. Story 13a's review found this shape in the voice's *per-call* path, where it is already closed with `math.isfinite` — story 14's extraction revealed that the *constructor* path has it in `half/crisis/classifier.py`, `half/correction/candidate.py` and `half/voice/gate.py` alike. Deliberately not fixed in 14: that story's whole value is that nothing changes, and a behaviour fix across three callers inside a refactor is how a refactor stops being reviewable. The asymmetry is preserved exactly and pinned by `test_the_constructor_predicate_still_admits_infinity_and_says_so`, so a later story has to change the pin deliberately rather than discover the behaviour.

- source_spec: `_bmad-output/specs/spec-half/stories/14-one-consultation.md`
  summary: The "no assertion moved" verification command cannot see an edit to a multi-line assertion, which is how the load-bearing one in `tests/test_correction.py` is written.
  evidence: `git diff <base> -- tests/ | grep -E '^[-+] *assert'` catches `tests/test_voice.py:1252` (a single-line assert) and misses `tests/test_correction.py:2477`, whose discriminating half — `name.startswith("half.model.port") or name == "half.model.port"` — is a continuation line inside a multi-line assert. Verified against the tree. The command was written into story 14's Verification section as the check that a refactor did not rewrite its own tests, and it is half-blind. Any future refactor story reusing it needs a diff-aware check over parsed assert statements rather than a line-prefix grep, or the reviewer must be told the grep is an aid and the commit message is the record.

- source_spec: `_bmad-output/specs/spec-half/stories/14-one-consultation.md`
  summary: The CAP-8 gate's floor drifted 24 cases behind its count, and its own comment claims the margin the drift disproves.
  evidence: Found during story 14. The gate collected 509 against a floor of 475 written when the count was 485 — a margin of 34 where the comment says 10. Twenty-four cases were added at some point without the floor following, which is exactly the failure the comment's own sentence ("the floor moves with them, so the margin does not grow") exists to prevent, and it means the gate now tolerates losing 34 cases rather than the 10 its author intended. Not story 14's doing: the implementer moved the floor with its own single case (476 -> 479, margin still 34) and recorded the drift rather than tightening another story's gate by 24 inside a refactor, which was the right call. Closing it means deciding deliberately what CAP-8's margin should be and whether the subsets below it carry the guarantees, in a change scoped to that.

- source_spec: `_bmad-output/specs/spec-half/stories/9e-the-disagreement-judge.md`
  summary: The nightly judgement is not batched, against `SPEC.md:124`'s constraint that the nightly pass runs "batched".
  evidence: `SPEC.md:124` — "The nightly pass runs on a cheaper model tier than conversation, batched, since cost is dominated by it and the free tier depends on that gap." The tier half is satisfied (pinned to `cheap` in review). The batching half is not: `half/consolidate/port.py`'s `async def disagree(one, other)` has no way to express a batch, and the model port's batch shape (`submit`/`collect`) is a different seam entirely. What ships is 24 sequential calls per main per night, one shared prefix, and no cache breakpoint — the prompt sits below the cheap tier's 4k minimum and the port refuses a marker the provider would ignore. Closing it means widening the `Disagreement` seam, which the story's Ask-First list forbids, so it needs its own decision.

- source_spec: `_bmad-output/specs/spec-half/stories/9e-the-disagreement-judge.md`
  summary: The per-main equip loop in `half/__main__.py` now exists in four copies, and the reason recorded for deferring its extraction has expired.
  evidence: `voices()`'s own docstring says a fourth consumer should not write one and names the shape it should become. Story 9e wrote the fourth (`judges()`), because the extraction is Ask-First and because it would fold in `second_opinion`, whose behaviour must stay byte-identical. The stated reason for waiting — "it belongs beside the consultation extraction rather than in front of it" — was discharged when story 14 shipped `half/model/consult.py`. Four callers, no remaining excuse, and the same argument story 14 made and proved: a correction made once should reach every caller.

- source_spec: `_bmad-output/specs/spec-half/stories/9e-the-disagreement-judge.md`
  summary: The CI workflow is edited by hand with unanchored replacements, and an edit that moves one gate's floor can silently move another.
  evidence: While moving `cap7_judgement`'s floor 95 -> 96, story 9e's implementer used an unanchored `str.replace` and Python replaced every occurrence — inflating the **AD-9 gate's floor** by one, a gate one step above it in the file and unrelated to the story. Caught by diffing every numeric change in `ci.yml` against the baseline and restored. This is the dead-anchor failure mode the project's probe discipline exists to catch, occurring in the tooling rather than in the code under test — which is the place nobody probes, because a floor that is one too high fails nothing until the case it silently now requires is deleted. Three probe-failure modes were catalogued in this story alone; this is the only one outside the code. Worth a deliberate fix: a floor that is derived rather than typed, or a check that every `-lt` line in the workflow matches a live collection.

- source_spec: `_bmad-output/specs/spec-half/stories/15a-what-a-message-is-worth-keeping.md`
  summary: Four cheap classifications run per inbound message — the second recurring spend in the product, after the nightly pass.
  evidence: Story 15a runs the four CAP-5 admission gates concurrently as four separate classifications per message, because no two gates share a label set and a single call answering for all four is a double that cannot be told apart from a working one. Stated in the module docstring rather than hidden. The saving is the batch shape, which `half/consolidate/judge.py` records the same deferral for: neither module has the seam, and `SPEC.md:124`'s "batched" applies to the nightly pass rather than to a turn. Worth a decision once a batch seam exists, since this one scales with conversation volume rather than with mains.

- source_spec: `_bmad-output/specs/spec-half/stories/15a-what-a-message-is-worth-keeping.md`
  summary: A derivation sits in front of that main's next message, because a main's worker takes one turn at a time.
  evidence: Derivation runs after the reply, so it never delays the reply it belongs to — but the actor's worker is serial per main, so the next message waits behind it. Bounded by the turn's own deadline rather than the morning's twenty seconds, and the four gate calls run concurrently rather than in series, both for this reason. Asserted on a clock rather than as a constant relation, because the arithmetic relation stays green for a serial build. Closing it properly means either deriving off the main's worker or accepting the coupling deliberately.

- source_spec: `_bmad-output/specs/spec-half/stories/15b-what-a-source-is-worth-keeping.md`
  summary: LAUNCH-RELEVANT — a real forwarded email defeats the echo defence entirely and is admitted as genuine independent corroboration.
  evidence: **Still open, and story 17 deliberately did not close it.** (This sentence used to read that `identity_set` "unions on exact normalized values of thread id, digest and sender" — it did not; there was no sender axis until story 17, which is the larger defect recorded below. The conclusion was right for the wrong reason.) `half/ingest/independence.identity_set` unions on exact normalized values of thread id, sender, digest and declared key. A real forward carries a `Fwd:` wrapper, so its scrubbed body differs from the original, its digest therefore differs, it sits in a new thread, and its sender is the forwarder — no shared identity on any axis, two independent groups, claim admitted. The manifest row and story 3's matrix both say "same content, different senders -> collapsed by content hash to one support", which holds only for byte-identical bodies. Latent since story 3 and inconsequential while nothing was admitted; consequential now that 15b admits claims. CAP-3's entire purpose is that "no claim is admitted from a single non-independent cluster of mentions", and a forwarded newsletter is the commonest such cluster in a real mailbox. Closing it needs near-duplicate collapse — a content-similarity problem 15b does not fund, and the implementer correctly did not invent one. Story 17 added the origin axis and left this hole open on purpose: a forward from a *different* person genuinely has a different origin, so no axis collapses it, and putting content similarity in that story would have hidden a small correct fix inside a large uncertain one. It is the last shape `tools/mailbox_sim.py` still miscounts and the last one `tools/admits_sim.py` still admits, so both simulations now measure exactly this and nothing else.

- source_spec: `_bmad-output/specs/spec-half/stories/15b-what-a-source-is-worth-keeping.md`
  summary: 15a's gate instructions are written for a message the main *sent* and are false for mail they *received*, which costs decision-relevance its load-bearing refusal label.
  evidence: `_OPENING` reads "You will be shown one message that person sent"; an ingested body is mail they received. And `A_REQUEST` ("addressed to the assistant") can essentially never fire on mail, so decision-relevance loses the refusal label 15a's own docstring calls load-bearing, leaving `WOULD_NOT_MATTER` to carry every newsletter alone. 15b imported the gates unchanged because changing them is Ask-First, which was right. Needs a mail-shaped `_OPENING` and a review of whether the four gates want per-ledger instructions or a shared spine with per-ledger openings.

- source_spec: `_bmad-output/specs/spec-half/stories/15b-what-a-source-is-worth-keeping.md`
  summary: A `-m` expression naming an unregistered marker deselects everything and exits 0, so a CI step or verification command can silently test nothing.
  evidence: 15b's Verification section carried `pytest -m "cap13 or cap3"`. `cap13` is registered nowhere; `--strict-markers` validates `@pytest.mark` decorators but not names inside a `-m` *expression*, so the command collects exactly what `cap3` alone collects (28 of 4746, verified) and reports success. This is the second broken verification command written into a spec this session — the first was a grep that could not see a multi-line assertion. Both were designated as the check for something important. Any `-m` expression in a spec or a workflow needs its marker names verified against `pyproject.toml`, or a check that every name used in a `-m` anywhere is registered.

- source_spec: `_bmad-output/specs/spec-half/stories/15b-what-a-source-is-worth-keeping.md`
  summary: LAUNCH BLOCKER — the vocabulary of what Half infers from a main's email was chosen inside an implementation story and needs an explicit human decision before any real inbox is read.
  evidence: `half/derive/revealed.py` ships six labels — `travels`, `pays_for_a_subscription`, `buys_things`, `keeps_appointments`, `does_paid_work`, `studies` — plus a refusal, an unsure label, and an instruction never to answer about health, faith, politics or sexuality ("a mailbox carries all of them"). The implementer flagged it unprompted as "product design I made inside an implementation story... the piece most likely to be wrong". It was forced rather than chosen: the frozen block forbids persisting a body in any form including a summary, and a generator holder would breach 15a's narrow-classifier rule, so a closed vocabulary was what remained — and it settled the within-run matching problem for free, since "two bodies yield the same claim" becomes exact equality on a constant. Vidit accepted it as a working set on 2026-09-04 with the explicit condition that it is revisited before launch. Two things need a person: whether these six are the right categories to infer about someone from their private mail, and whether the exclusion list is complete. Cheap to change — closure is checked at import and every member is checked against the instructions. Same class of blocker as the clinical review of the crisis protocol and the native-speaker review of the correction tables.

- source_spec: `_bmad-output/specs/spec-half/stories/5c-the-interruption.md`
  summary: The interruption gate has no urgency source and no writer, so it is a rule nothing can trigger and nothing records.
  evidence: Deliberate, and named in the story's Intent — Half cannot currently know an option is closing, because no record carries a horizon. Two consequences follow. `Wiring.interrupt` is held and never run, which is story 5b's shape (a module that waited a story to become real). And nothing appends: `half/surface/touch.raised` already exists and its docstring says it is the shape CAP-10's interrupt will use (it raises a loop and spends no day), but the gate's own-bound input is therefore always `None` in production — honest, since nothing can produce an interruption either, but the bound is a rule with no source until a judge exists. Closing it needs a horizon on a record (Ask First) or derivation that produces one, plus the writer.

- source_spec: `_bmad-output/specs/spec-half/stories/5c-the-interruption.md`
  summary: `JUDGEMENTS = 3` and `BOUND_SECONDS = 5.0` in the interrupt gate are chosen rather than derived, and the implementer flagged them for argument.
  evidence: Both are pinned by value in a test with the reasoning written beside them, following `consolidate/mint.py`'s convention for its own `24`. `BOUND_SECONDS` matches `consolidate/judge.py`'s and is spelled locally rather than imported, so `half/interrupt` takes no name from a provider package. `INTERRUPTION_DAYS` is the only constant here that is derived (`PERIOD_DAYS[Timescale.DAYS]`, the same table and the same sentence as `trust.stakes.INTERRUPTION_DAYS`). Worth an argument once a real urgency source exists and the numbers can be measured against something rather than reasoned about.

- source_spec: `_bmad-output/specs/spec-half/stories/5c-the-interruption.md`
  summary: `half/interrupt/port.py` qualifies for `tests/test_purity.py::PURE_MODULES` and was deliberately left out.
  evidence: The implementer declined to add it because that list carries another story's CI floor, and editing a floor-bearing list inside an unrelated story is the "unanchored edit moves another gate" failure story 9e hit for real. One line, whenever it is done deliberately.

- source_spec: `_bmad-output/specs/spec-half/stories/5d-the-unsaid.md`
  summary: `tests/test_ladder.py::governed_writes` reads explicit keywords only, so a `**` splat walks past the ladder's write monopoly.
  evidence: `store.record(Op.ASSERT, "b_1", t, **{"license": "assert"})` returns no findings, while `ceiling_omissions` twenty lines away in the same file explicitly treats `kw.arg is None` as a failure. Verified: a splat's `kw.arg` is `None`, so a scan looking for a named keyword sees nothing to check. The 5d implementer found it and deliberately did not fix it, because production legitimately splats `**ladder.admitted(...)` in four places and closing the hole needs an allowance for ladder-call splats — not 5d's to design. This is what makes the "only the ceiling is reachable" finding below softer than it reads: the write monopoly is one `**` away from being bypassable.

- source_spec: `_bmad-output/specs/spec-half/stories/5d-the-unsaid.md`
  summary: Only one of the unsaid queue's three release conditions is reachable through this product's own doors; the other two require a foreign or legacy log.
  evidence: `admitted()` always births at `behave` with no argument that raises it; `promote(to=ASSERT)` requires `acknowledged is True` **and** a receipt, and writes `known_to_main=True` itself; `demote` only lowers and carries fields forward; `quarantine` writes `behave`; and the fold's `Op.ASSERT` case carries forward only `STICKY`, so a re-assert without ladder fields drops to `behave`. A belief sitting at `assert` while missing a precondition can therefore only arrive from outside this build. That is a real operational case — `own_rung`'s demotion branch exists for it and runs on the turn's reply path — but it means the queue in practice reports beliefs held by a **ceiling** and nothing else, which is narrower than CAP-10 implies. The suite runs every ceiling case end to end over a real log and asserts the other two over belief mappings; the module and test docstrings say which is which rather than blurring them.

- source_spec: `_bmad-output/specs/spec-half/stories/15b-what-a-source-is-worth-keeping.md`
  summary: LAUNCH BLOCKER — 15b's frozen block over-constrained AD-13 and made CAP-2's success criterion unreachable; Vidit has approved loosening it, and the work is unbuilt.
  evidence: 15b's Never list read "No body persisted anywhere, in any form, including a summary or an embedding." AD-13's actual rule is narrower — "message bodies are never persisted. A body is normalised, scanned, handed to its consumer, and discarded in memory" — and its own accepted-cost note ("rebuild can no longer re-derive claims from original text") presumes claims are derived from bodies and persisted. The "including a summary" clause was mine, not the architecture's. Its consequence propagated: 15b could only write claims from a closed six-label vocabulary Half owns (travels, pays_for_a_subscription, buys_things, keeps_appointments, does_paid_work, studies), and story 7 found those six are the complete set of statements a demonstration can ever make from mail — so CAP-2's "confirmed as true **and previously unstated by the main**" cannot be met, because nobody learns that they travel. Vidit decided on 2026-09-04 to loosen the clause to AD-13's actual rule: a claim derived from *scrubbed* text, in Half's own words, is not a body. Two known costs, both unbuilt: it needs a generator holder rather than a closed-set classifier, and it reopens the claim-matching problem the closed vocabulary solved for free (two bodies yielding "the same claim" stops being exact equality on a constant). Supersedes the earlier entry recording the six-label vocabulary as needing sign-off — the vocabulary is now known to be the wrong shape, not merely unreviewed.

- source_spec: `_bmad-output/specs/spec-half/stories/7-the-demonstration.md`
  summary: The ninety-second budget does not fit; measured, it admits about five messages worst case.
  evidence: Measured by story 7's implementer against the shipped bounds, not estimated. 15a's four gates run concurrently under one 5s bound; 15b's reading is a second 8s bound; bodies are read in series because `Pipeline.ingest` awaits its consumer inside the `async for`. So 90 − 20 (compose reserve) = 70s ÷ 13s = five messages worst case, against `PER_RUN = 200`. A first pull that must find two independent groups behind one label inside five messages will often find nothing, which makes "nothing to offer" the ordinary first-run outcome rather than an edge case. The behaviour when it does not fit is honest and built (the source stops yielding at its share so every receipt survives; past the budget the claim stays in the ledger and nothing is said) and pinned by a case that fails if a bound moves. The fix is a batch seam in `half.model` — the same deferral `consolidate/judge.py` and 15a both record — and is not a number that can be tuned here.

- source_spec: `_bmad-output/specs/spec-half/stories/7-the-demonstration.md`
  summary: Story 7 added a negative table to `half/correction/signals.py`, inverting a rule story 12 states deliberately.
  evidence: Story 12's module says "there is deliberately **no negative table**", which is right for a *candidate*: doing nothing is the safe direction when Half inferred something on its own. For an *offer* the asymmetry reverses — reading a main's "no" to the first thing Half ever said as "said nothing" discards the correction they are most likely to make. Story 7 added `DECLINE_SOURCE`/`is_decline`, whole-message only and never consulted by `recognize` (asserted: `recognize` fires on none of its rows). The implementer flagged it as arguably Ask-First rather than deciding quietly. The reasoning is sound and the containment is real; what wants a decision is whether story 12's stated rule should now record the exception, so a later reader does not find the module contradicting itself.

- source_spec: `_bmad-output/specs/spec-half/stories/15c-a-claim-worth-hearing.md`
  summary: DECIDED — the near-quotation floor for a generated revealed claim is four words, not story 4b's adjacent pair.
  evidence: 15c's Never list said "no quotation of a source's wording" without naming a unit, and this tree already owns a reviewed near-quotation rule: 4b's adjacent word pair, which `half.voice.leak` imports rather than restates. Applied here at length two it deletes the capability — a revealed claim's job is the particulars, and a place, date or service name is exactly the two-word run it shares with the mail, so "flies to Delhi most months" would quote any email containing "to Delhi". The implementer kept 4b's *unit* (imported, matras intact, invisible characters removed) and made only the *length* this caller's, and flagged it as needing a human rather than deciding quietly. Accepted on 2026-09-05: four consecutive words matching a source is a meaningful quotation; two is a preposition and a proper noun. Worth revisiting if a real mailbox shows claims reproducing distinctive phrases at three.

- source_spec: `_bmad-output/specs/spec-half/stories/15c-a-claim-worth-hearing.md`
  summary: A later pull pays for a generation it then throws away when the ledger already holds that label's belief.
  evidence: A second pull crossing the same label writes a sentence, and the append then finds the belief id already present. Bounded at one wasted generation per label per pull (at most six). Closing it means telling a `Run` which belief ids the ledger already holds, and the only honest route is a constructor parameter — which is exactly what `Run.__init__` deliberately has none of, so that no call site can hand it a threshold either. The implementer pinned the waste with a test so it is a number rather than a docstring.

- source_spec: `_bmad-output/specs/spec-half/stories/15c-a-claim-worth-hearing.md`
  summary: A CAP-4 guard forbids exception messages in `half/context/build.py`, with a blast radius wider than its subject.
  evidence: `test_no_literal_question_string_exists_anywhere_on_the_question_path` refuses any phrase-shaped string constant in that module and does not distinguish a template from a `ValueError` message. 15c's new `runs()` helper had to spell its refusal `f"runs:length={length!r}"` and put the sentence in the docstring. The rule is right — no English question template on the question path — but it also silently forbids readable error text in a module that has legitimate reasons for some. Worth narrowing to what actually reaches a main.

- source_spec: `_bmad-output/specs/spec-half/stories/3-ingestion-and-secrets.md`
  summary: RESOLVED by story 17 — the independence machinery had lost the origin axis in extraction, so two emails from one sender counted as two independent supports, and the fixtures hid it by passing a `sender` field nothing read.
  evidence: Measured, not inferred. `IDENTITY_FIELDS` is `("thread_id","thread"), ("digest","content"), ("independence_key","declared")` — there is no sender or origin axis. So `independent_groups` returns **2** for two different messages from one sender in two threads, and CAP-3's bar is two. A shop's newsletter corroborates itself; an airline's booking confirmation and its check-in reminder are "two independent sources". Both upstream sources say otherwise: the extraction manifest records the machinery as collapsing "sources sharing **origin**, content hash, or **publisher**", and story 3's own frozen block says "union-find over **origin**, content hash, and declared key". The implementation substituted *thread* for *origin* and dropped publisher. What kept it invisible: `tests/test_independence.py`'s `source()` helper takes and stores a `sender` that `identity_set` never reads, so every fixture looks like it exercises a sender axis — including `test_a_forward_from_another_sender_collapses_by_content`, which varies sender across ten messages sharing one thread and passes because of the *thread*. This is strictly larger than the forwarded-email hole recorded earlier, and that hole is partly a symptom of it. Fixing it will reduce admitted claims wherever one sender was doing the corroborating, which is the correct direction and will change 15b/15c counts. **Closed by story 17** (`716514d`, `627f06e`, `5a6fa7e`): `IDENTITY_FIELDS` gains `("sender", "origin")` alongside the thread rather than in place of it, `Candidate` carries the receipt's sender and `identity()` supplies it, and nothing parses an address — matching stays `_normalize`'s. Measured either side: `tools/mailbox_sim.py` went from 2 of 5 shapes miscounted to 1 of 5 (the newsletter 8 -> 1); `tools/admits_sim.py`'s newsletter went from counted 2 / admitted 1 to counted 0 / admitted 0, with airline + hotel unchanged at 2 and 1. One correction to the entry above, recorded because the intent block of story 17 repeats it: the axis was not *merely* lost in translation. Story 3's implementation left a comment in `IDENTITY_FIELDS` saying sender was "deliberately **absent**" because unioning on it is transitive, and pinned that with `test_a_shared_sender_does_not_collapse_distinct_threads`, which asserted thirty messages from one address across ten threads were **ten** supports. So this was a deliberate decision contradicting story 3's own frozen block, not an omission — and story 17 reverses it. The transitivity the old comment warned about is real and is now the rule; what bounds it is that the axis only ever collapses sources sharing a value, which `test_ten_senders_across_ten_threads_at_scale_stay_ten_supports` pins at the same size.

- source_spec: `_bmad-output/specs/spec-half/stories/16-the-mailbox-reached.md`
  summary: LAUNCH-RELEVANT — Gmail's list route returns newest-first and offers no sort control, so `MailSource`'s stated "oldest first, so a cursor can advance monotonically" is not satisfied by the shipped source and cannot be satisfied by any transport. Story 16's own matrix asks the transport for oldest-first; the transport declined, because doing it there would look like the contract without being it.
  evidence: Measured against the API's shape, not inferred. `users.messages.list` has no `orderBy` parameter and returns most-recent-first; `GmailSource.fetch` yields stubs in list order and adds no ordering of its own; `MailSource` (`half/ingest/port.py`) documents "Yield messages newer than `since`, oldest first. Ordered so a cursor can advance monotonically and a failure part-way leaves everything before it already captured." Reversing a page inside the transport does **not** produce the contract — page one still holds the newest block — so it would be a decision that hides the defect. Two consequences today. (1) `half.__main__.Bounded` cuts the pull at `PULL_SECONDS`, and `half.onboard.flow.messages_that_fit()` puts the worst case at a handful of messages, so `half onboard` reads only the newest few of a mailbox and never reaches history — which is where a revealed claim's two independent groups would come from. (2) The moment anything persists `Ingested.cursor` (nothing does yet; `half/__main__.py:592` uses it only as a record stamp), a truncated newest-first pull sets the cursor to the newest message's timestamp and every older message is skipped for ever — exactly the failure `half.ingest.gmail._iso_from_internal_date` refuses to cause by fabricating a "now". The fix is a rule in `gmail.py`, which is tested: either walk `MAX_PAGES` to the end and yield the pages in reverse before emitting, with a stated memory bound, or make the cursor mean "the oldest message not yet read" so a newest-first walk is resumable backwards. Both are ingestion rules and neither belongs in a transport. `tests/test_gmail_transport.py::test_the_transport_hands_over_the_provider_s_own_order_untouched` pins the honest property in the meantime.

- source_spec: `_bmad-output/specs/spec-half/stories/16-the-mailbox-reached.md`
  summary: The HTTP client a pinned dependency already pulls in is not usable, so the Gmail transport uses `urllib.request` on a worker thread. AD-2's gate reads declared dependencies, and `httpx` is only transitive.
  evidence: `uv.lock` carries `httpx` as a dependency of `python-telegram-bot` and `httpx2` as one of `anthropic`; neither is declared in `pyproject.toml`. `tests/test_dependencies.py::test_the_runtime_imports_only_stdlib_and_declared_dependencies` builds its allow-list from `packages_distributions()` over the **declared** distributions, so any `import httpx` under `half/` red-builds CI — verified by probe m12, which planted one in `pipeline.py` and turned that gate red. Declaring it is a new runtime dependency and therefore Ask First. Reaching httpx through `telegram.request.HTTPXRequest` would make a mail source depend on the chat channel's SDK, and that SDK is itself migrating off httpx — the same lock file already pulls `httpcore` for python-telegram-bot on python >= 3.14. If a future story wants a connection-pooled async client, the decision to make is whether `httpx` becomes a declared dependency, not whether the transport can borrow one.

- source_spec: `_bmad-output/specs/spec-half/stories/16-the-mailbox-reached.md`
  summary: There is no way to put a Gmail token into the `SecretStore` from the command line, so `half onboard`'s prerequisite has no shipped command.
  evidence: `half onboard <main>` reads `gmail_access_token` through `HttpTransport.from_secrets` and refuses plainly when it is absent, naming the credential and the directory it belongs in. Putting it there means writing `<HALF_ROOT>/../.credentials/<main_id>.json` by hand or calling `FileSecretStore.put` from Python. Not built here: story 16's Code Map names one subcommand, and a `half secret put` that takes a value on `argv` would put a live credential in shell history and in `ps` output, which is a design question rather than a missing line. Worth a small story that reads the value from stdin.

- source_spec: `_bmad-output/specs/spec-half/stories/16-the-mailbox-reached.md`
  summary: A Gmail access token lives about an hour and nothing refreshes it, so `half onboard` needs a credential minted shortly before it runs.
  evidence: The transport treats 401 as `MailboxNotAuthorised` — permanent, never retried — which is correct for a rejected credential and is what an expired one looks like. Refreshing needs a refresh token, a client id and a client secret, which is the interactive OAuth consent flow story 3 deferred and story 7 declined, and story 16 lists as Ask First. Recorded so that the first real run's "it worked yesterday" is a known shape rather than a mystery.

- source_spec: `_bmad-output/specs/spec-half/stories/16-the-mailbox-reached.md`
  summary: The Gmail transport puts no ceiling on a response body's size, deliberately, and reads it whole into memory.
  evidence: Considered and rejected while writing it. A byte cap raises on the message that exceeds it; that fault crosses `GmailSource._call` as a `ChannelError` and aborts the whole pull, and the next pull re-fetches the same message and aborts again — one oversized message would wedge a mailbox permanently. Skipping it silently instead is the hidden decision the story forbids outright. So the honest options are a rule in `gmail.py` (which is tested) that can skip a message and *say* it did — the shape `Ingested.skipped_unreadable` already has — or streaming the parse. Neither is a transport's to make. The exposure is bounded in practice by Gmail's own 35MB message limit and by `format=full` returning attachment data by reference above a threshold, but it is not bounded by anything Half wrote.

- source_spec: `_bmad-output/specs/spec-half/stories/16-the-mailbox-reached.md`
  summary: `half onboard` cannot be driven end to end through `main()` in the suite, because the notice has to reach a real platform before a mailbox is connected.
  evidence: Story 7's ordering — the main is told before a source is touched — means a demonstration that reads mail must first send over `TelegramChannel`, which `build()` constructs with the real `PTBTransport`. `main()` takes no channel, so the offline path through it stops at `Reason.NOT_TOLD`. The integration is asserted one layer down instead, against `half.__main__.onboarded` with the channel doubled and the HTTP layer faked (`tests/test_gmail_transport.py::test_the_shipped_composition_reaches_a_mailbox_and_writes_a_receipt`), which covers everything except `main`'s own argument handling — and that is covered separately. A `build(config, token, *, channel=None)` seam would close the gap; it is a change to the composition root's signature and was not this story's to make.

- source_spec: `_bmad-output/specs/spec-half/stories/16-the-mailbox-reached.md`
  summary: LAUNCH BLOCKER — `MailSource.fetch` promises oldest-first and Gmail cannot deliver it, so onboarding reads only the newest handful of a mailbox and can never reach history.
  evidence: The first thing real contact has broken, and it is a contract rather than a measurement. `half/ingest/port.py` states "Yield messages newer than `since`, **oldest first**. Ordered so a cursor can advance monotonically and a failure part-way leaves everything before it already captured", and `GmailSource`'s own docstring repeats it. Gmail's list route has no `orderBy` and returns newest-first; the story-16 implementer established that reversing each page does not produce the contract, because page one still holds the newest block — it would look like the promise without being it — and refused to write it. Combined with story 7's `messages_that_fit()` of about five, `half onboard` sees roughly the five newest messages and no history at all. Worse, the moment anything persists `Ingested.cursor` (recorded as `newest`), every older message is skipped for ever. The resolution is a spec decision, not a patch: either buffer to honour oldest-first, or change the port's contract to newest-first-and-bounded and make the cursor a forward-only watermark. Newest-first may in fact be the better product answer for a demonstration — recent life is what you would demonstrate on — but `GmailSource`'s docstring is false today either way.

- source_spec: `_bmad-output/specs/spec-half/stories/16-the-mailbox-reached.md`
  summary: A deferred-work entry written by the lead during story 16 was swept into that story's commit `aa02e20` by a `git add -A`.
  evidence: The entry (about story 3's independence machinery losing the origin axis) is correct and correctly placed, but it is committed under a message describing the Gmail transport. Cause: the lead wrote to the repository while an implementation agent was working in it, having identified that exact risk earlier in the session when sequencing stories 11 and 12. Recorded so the commit history is not silently misleading; not rewritten, because the content is right and rewriting shared history to fix an attribution is the larger harm.

- source_spec: `_bmad-output/specs/spec-half/stories/17-the-axis-that-went-missing.md`
  summary: RESOLVED IN FLIGHT — story 17's first build shipped the outage story 3's author had predicted, and it was caught by measurement before it was pushed.
  evidence: The lead's spec said the origin axis "was lost between the specification and the implementation." It was not: story 3's implementation carried a comment saying the sender was *deliberately absent*, because unioning on it is transitive — a handful of people appearing in two threads chain nearly everything, corroboration trends to one group, and the gate never opens — and pinned that with a test asserting thirty messages from one address across ten threads were ten supports. The implementer flagged the deleted comment as "accepted rather than answered" rather than proceeding quietly. Measured against the shipped function: 200 messages / 60 people / 120 threads collapsed to **1** group; 500/80/200 to 1; every realistic density to 1. The anti-outage test could not see it — its own docstring reads "one sender per thread. Nothing is shared across a thread boundary, so nothing crosses one", so the chaining that causes the collapse was structurally excluded. The lead had specified the wrong outage (an empty origin, a lesser failure) and the fixture matched the specification faithfully. Fixed by applying the axes at two levels that cannot chain, with a third clause added by the implementer because the literal two-level rule over-counted 500 messages from ten people as 142 supports — the mirror failure. `tools/percolation_sim.py` carries both rejected rules so the trade can be overturned with numbers. The anti-outage case now runs the rejected rule first and asserts it returns 1 on the same 360 sources before asserting the shipped rule returns 60.

- source_spec: `_bmad-output/specs/spec-half/stories/17-the-axis-that-went-missing.md`
  summary: Independence over-counts by roughly 2x at mailbox scale, though not at claim scale where CAP-3 actually reads it.
  evidence: Measured on the shipped build over synthetic mailboxes: 500 messages / 80 people / 200 threads returns 160 supports against roughly 80 distinct voices; 500/20/200 returns 53 against roughly 20. The exact rows (500/10/400 returns 10) and the claim-sized shapes in `tools/mailbox_sim.py` are all correct, and CAP-3 counts supports for one claim rather than for a mailbox — so this does not currently admit anything on thin evidence. Recorded because the number is wrong in a direction nobody is watching, and a later feature that reads independence over a whole mailbox would inherit it.
